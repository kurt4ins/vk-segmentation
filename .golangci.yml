version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: standard
  enable:
    - revive
    - bodyclose
    - rowserrcheck
    - sqlclosecheck
    - nilerr
    - misspell
  settings:
    revive:
      rules:
        - name: exported
          disabled: false
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/kurt4ins/vk-segmentation

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
